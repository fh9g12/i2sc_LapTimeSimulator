function varargout = genCarAeroData(varargin)
% genCarAeroData - student-facing alias for open.genCarAeroData (combine
% a front and rear wing choice into whole-car CL, CD and aero balance).
% See "help open.genCarAeroData" for full usage, options and sign
% conventions.
    [varargout{1:nargout}] = open.genCarAeroData(varargin{:}) ;
end
